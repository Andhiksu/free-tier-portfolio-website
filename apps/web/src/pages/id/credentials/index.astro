---
import CredentialsPage from "../../../components/pages/CredentialsPage.astro";
import { getSiteContent } from "../../../lib/sanity/loader";

const content = await getSiteContent();
---

<CredentialsPage
  locale="id"
  homeData={content.homeData.id}
  credentials={content.credentials.items}
/>
